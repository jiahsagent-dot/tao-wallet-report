export const metadata = {
  title: 'My Portfolio — Tao Wallet Report',
  description: 'Combined wallet view across root + subnet coldkeys.',
  robots: { index: false, follow: false },
};

export default function MeLayout({ children }) {
  return children;
}
